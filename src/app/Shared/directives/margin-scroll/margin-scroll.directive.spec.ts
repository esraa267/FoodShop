import { MarginScrollDirective } from './margin-scroll.directive';

describe('MarginScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new MarginScrollDirective();
    expect(directive).toBeTruthy();
  });
});
